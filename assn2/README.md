# Race Tracking Server

To run, start the tracking server, then 1 or more clients, then the simulator,
all using the default ports

	$java -jar TrackingServer.jar
	$mono Client.exe
	$mono Simulator.exe

